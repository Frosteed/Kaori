import { links } from '../../data'
export const Support = () => {return window.location.href = links.supportServer}
export const Invite = () =>{return window.location.href = links.invite}
export const Vote = () =>{return window.location.href = links.vote}